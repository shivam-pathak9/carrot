# carrot


Carrot is a lightweight, Redis-inspired server written in Go. It is designed as a learning project for understanding how a networked key-value server works, including TCP connections, the RESP protocol, command parsing, and command execution.
